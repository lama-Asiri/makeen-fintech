MAKEEN - TODO LIST
==================

AUTH
[x] 1. Add forgot-password and reset-password endpoints to backend
[x] 2. Connect ForgotPassword and ResetPassword forms to backend
[x] 3. Test forgot password and reset password full flow end-to-end

BACKEND CLEANUP
[ ] 3. Clean up PythonClasses/ folder — move models into routers as Pydantic models
[ ] 4. Decide on database/connection.py — keep SQLAlchemy or remove in favour of Supabase client

DATABASE CONNECTIONS (features currently not saving to DB)
[ ] 5. Chat history — currently localStorage only, save/load from chat table per user
[x] 6. Edit Profile — display name + avatar saved to public."User" and Supabase Storage, loaded from DB on mount
[x] 7. Test edit profile — change display name, change avatar, verify both persist after page refre[ ] 8. Feedback modal — onSubmit does nothing, save to rating table (needs POST /feedback endpoint)
[ ] 8. Report bug modal — console.log only, save reports to DB or send via email
[ ] 9. File uploads — save actual file to Supabase Storage, not just metadata in chat object

AI / CHAT FUNCTIONALITY
[ ] 10. Integrate xAI (Grok) or chosen LLM — add API key to backend .env, create POST /chat/message endpoint that forwards messages to the model
[ ] 11. Stream AI responses — use SSE or chunked response so the reply appears word-by-word instead of all at once
[ ] 12. Persist chat history to DB — save each message (role, content, timestamp) to a chat/messages table linked to the user
[ ] 13. Load chat history on login — fetch previous conversations from DB so history survives page refresh
[ ] 14. Multi-conversation support — allow users to start new chats and switch between past conversations (conversation_id per session)
[ ] 15. File upload to AI — send uploaded file content to the model as context (text extraction / base64 for images)
[ ] 16. System prompt / persona config — define Makeen's personality and context in a server-side system prompt

COMPLETED
==================
[x] Fix service role key in backend .env
[x] Move backend URL to frontend env variable (VITE_API_URL)
[x] Auth context + session persistence across page refreshes (Supabase JWT, no extra cookie work needed)
[x] Loading spinner while checking session on startup
[x] Backend restructured into routers/ and core/
[x] Delete 27 unused files from frontend/src/imports/
[x] Sign up stores user in public.user via DB trigger
[x] Login blocks unverified users
[x] Email confirmation required before sign in
